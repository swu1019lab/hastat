[default]
bbox = [0.2, 0.2, 0.8, 0.6] # all artist objects will be plotted in the boundary box (relative to matplotlib axes)
xy = [0, 0.4] # the anchor point to plot gene (relative to bbox)
width = 1 # the width to plot gene, should be within [0, 1] (relative to bbox)
height = 0.05 # the height to plot gene, should be within [0, 1] (relative to bbox)
color = 'C0' # the color to plot feature
feature = 'exon' # the feature type that the target gene contain, e.g., CDS, exon or five_prime_UTR
gene_label_size = 10 # the fontsize of gene label
gene_label_show = true # Whether to display the gene label

ann_file = "" # the data of variant annotation, csv file, e.g., hap_ann.csv
ann_y = 0.5 # the y value of annotation text (relative to bbox)
ann_text_size = 8 # the fontsize of annotation text

hap_file = "" # the data of gene haplotypes, csv file, e.g., hap_table.csv
hap_y = 0 # the y value of haplotypes (relative to bbox)
hap_height = 0.3 # the height of whole haplotypes plot (relative to bbox)
hap_cols = [] # only show the haplotypes data of interested
hap_label_show = true # Whether to display the haplotype labels
hap_label_size = 8 # the fontsize of haplotype labels

hap_group_file = "" # the group information of haplotypes, csv file with 2 columns: haplotype, group
hap_group_bar_x = -0.12 # x offset for group bar relative to haplotype matrix
hap_group_bar_width = 0.02 # width of group bar

hap_freq_show = true # whether to show variant frequency
hap_freq_height = 0.05 # height of frequency track
hap_freq_y = 0 # the y value of frequency track (relative to bbox)
hap_freq_plot_type = "bar" # plot type of frequency track: "bar" or "pie"

hap_cluster_show = true # whether to show hierarchical clustering dendrogram
hap_cluster_linkage = 'ward' # hierarchical linkage method: 'ward', 'complete', 'average', 'single'
hap_cluster_width = 0.1 # width of the dendrogram relative to the plot width

hap_het_rate = [0, 1] # the range of heterozygosity rate for haplotypes (samples) to keep
hap_mis_rate = [0, 1] # the range of missing rate for haplotypes (samples) to keep
var_het_rate = [0, 1] # the range of heterozygosity rate for variants (sites) to keep
var_mis_rate = [0, 1] # the range of missing rate for variants (sites) to keep

pi_file = "" # the pi data of target region, csv file, e.g., gene.pi.csv with chrom,start,end,n_bases,counts,pi,groups
pi_y = 0.5 # the y value of pi plot (relative to bbox)
pi_height = 0.15 # the height of pi plot (relative to bbox)
pi_style = "line" # plot style: "line" or "fill" (fill_between)

fst_file = "" # the fst data of target region, csv file, e.g., gene.fst.csv with chrom,start,end,counts,fst,groups
fst_y = 0.75 # the y value of fst plot (relative to bbox)
fst_height = 0.15 # the height of fst plot (relative to bbox)
fst_style = "fill" # plot style: "line" or "fill" (fill_between)



[BnaA02G0156900ZS]
hap_file = "results/BnaA02G0156900ZS.FT1007.table.csv"
pi_file = "results/BnaA02G0156900ZS.FT1007.pi.csv"
fst_file = "results/BnaA02G0156900ZS.FT1007.fst.csv"


